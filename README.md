# ClassificadordeEmails
Automatizador de leitura e classificação de emails.

Instruções para executar aplicação:

-Baixe o python

-Execute este comando na raiz do projeto para instalar as dependencias:  pip install -r requirements.txt

-Execute este comando na raiz do projeto para executar o projeto:  python app.py
